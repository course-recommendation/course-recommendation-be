spring:
  application:
    name: CourseRecommendation

  config:
    import:
      - classpath:jwt.yaml
      - classpath:domain.yaml
      - classpath:datasource.yaml

server:
  port: 8082

logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter: DEBUG